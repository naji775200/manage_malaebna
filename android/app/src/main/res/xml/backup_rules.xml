<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude specific preferences that should not be backed up -->
    <exclude domain="sharedpref" path="device_info.xml"/>
    <exclude domain="database" path="temp_data.db"/>
    <exclude domain="file" path="temp/"/>
</full-backup-content> 